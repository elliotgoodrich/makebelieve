# Tool compatibility

Whether common tools can read files that are generated on-the-fly, or whether
they only read the 1 byte initially reported by `makebelieve` when that file
is `stat`. Generated by [`experiments/tool_compat`](experiments/tool_compat).

## Linux

| Tool | Flags tested | Compatible |
| --- | --- | --- |
| `awk` | `'END { print NR, length($0) }'` | Yes |
| `cat` | none | Yes |
| `cmp` | none | Yes |
| `cp` | none | Yes |
| `dd` | `if=FILE of=DEST status=none` | Yes |
| `diff` | none; `-q` | Yes, except `-q` |
| `grep` | `-c` | Yes |
| `head` | `-c 500` | Yes |
| `less` | `-F -X` | Yes |
| `md5sum` | file on standard input | Yes |
| `od` | `-c` | Yes |
| `python3` | `open().read()`; `mmap` (read-only); `os.open()` then `os.fstat()` | Yes |
| `rsync` | none | Yes |
| `sed` | `-n '50,52p'` | Yes |
| `sha256sum` | file on standard input | Yes |
| `sort` | `-r` | Yes |
| `stat` | `-c %s` | No |
| `tail` | `-n 3`; `-c 200` | Yes |
| `tar` | `-cf`, then `-xf` | Yes |
| `wc` | `-c -l`, file on standard input | Yes |
| `xxd` | none | Yes |

## Windows

The Unix tools here (`cat`, `cmp`, `diff`, `md5sum`, `tail`, `wc`) are the
ones that ship with Git for Windows.

| Tool | Flags tested | Compatible |
| --- | --- | --- |
| `cat` | none | Yes |
| `certutil` | `-hashfile FILE MD5`; `-hashfile FILE SHA256` | Yes |
| `chrome` | `file://` URL; `http://` URL served by `python -m http.server` | Over `http://` only: see [below](#chrome) |
| `cmake` | `-E cat`; `-E copy`; `-E sha256sum`; `-E compare_files` | Yes, except `-E compare_files` |
| `cmp` | none | Yes |
| `comp` | `/M` | No |
| `copy` (`cmd`) | `/y` | Yes |
| `Copy-Item` | none | Yes |
| `curl` | `file://` URL | Yes |
| `diff` | none; `-q` | Yes |
| `fc` | none; `/b` | No: see [below](#fc) |
| `find` (`cmd`) | `/c` | No |
| `findstr` | `/c:` | Yes |
| `Get-Content` | none; `-Raw` | Yes |
| `Get-FileHash` | `-Algorithm SHA256` | Yes |
| `Get-Item` | `.Length` | No |
| `git` | `hash-object --no-filters` | Yes |
| `md5sum` | none | Yes |
| `more` (`cmd`) | file on standard input | Yes |
| `node` | `fs.readFileSync`; `fs.createReadStream` | Yes |
| `python` | `open().read()`; `mmap` (read-only) | Yes |
| `robocopy` | `/R:0 /W:0`; `/R:1 /W:1` | Yes, but see [below](#robocopy) |
| `Select-String` | `-Pattern` | Yes |
| `System.IO.File` (.NET) | `ReadAllBytes`; `Copy`; `OpenRead().Length` | Yes |
| `tail` | `-c 100` | Yes |
| `tar` | `-cf`, then `-xf` | No |
| `type` (`cmd`) | none | Yes |
| `wc` | `-c -l` | Yes |
| `xcopy` | `/y /q` | Yes |

## Notes

### `chrome`

Opened as a `file://` URL, Chrome gets each file's size by path before opening
it and then reads exactly that many bytes. The first load of a page whose
files have never been built renders blank.

### `fc`

`fc` upper-cases the paths it is given before opening them. The mount is
case-sensitive, so the upper-cased name is not found, and `fc` fails with
"cannot open" even for an output that has been built.

### `robocopy`

Correct in the experiment, but run on its own straight after mounting it has
repeatedly left a 1-byte copy and still exited 0, after reporting
`ERROR 1 (0x00000001) Changing File Attributes` on the destination directory.
The cause is not known.
